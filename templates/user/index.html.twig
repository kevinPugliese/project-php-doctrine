{% extends 'base.html.twig' %}

{% block body %}

    <ul>

        {% for user in users %}
            <li>{{ user.name }}</li>
            <li>{{ $users->getAddress() }}</li>
        {% endfor %}
    </ul>

{% endblock %}
